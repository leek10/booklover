from setuptools import setup

setup(name = 'hw09booklover',
      version = '1.0',
      description = 'A package for book lovers to add their books to a reading list',
      url = 'https://github.com/leek10/hw09booklover',
      author = 'Kayla Lee',
      license = 'MIT',
      packages = ['bookloverpackage'],
      install_requires = ['pandas'])
